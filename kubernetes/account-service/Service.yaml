apiVersion: v1
kind: Service

metadata:
  name: account-service
  namespace: banking

spec:
  type: ClusterIP

  selector:
    app: account-service

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      