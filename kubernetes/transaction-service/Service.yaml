apiVersion: v1
kind: Service

metadata:
  name: transaction-service
  namespace: banking

spec:
  selector:
    app: transaction-service

  ports:
  - port: 5003
    targetPort: 5003

  type: ClusterIP